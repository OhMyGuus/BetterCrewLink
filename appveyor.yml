version: '{build}'
image:
- Visual Studio 2019
build_script:
- pwsh: >-
    yarn install
    
    yarn run dist --publish never
    
artifacts:
  - path: dist/win-unpacked
    name: unpacked
    type: zip
  - path: dist/win-unpacked
  - path: 'dist/Better-CrewLink Setup*.exe'
    name: installer
    type: exe
cache:
  - node_modules
